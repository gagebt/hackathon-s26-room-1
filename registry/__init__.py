"""registry — живой реестр: второй прогон делает его умнее, а не длиннее.

    merge(base, new) -> Graph

Обновляет существующее вместо создания дубля, закрывает выполненное,
снимает отменённое с каскадом по DERIVED_FROM и накладывает решения человека.
"""

from .merge import merge

__all__ = ["merge"]
