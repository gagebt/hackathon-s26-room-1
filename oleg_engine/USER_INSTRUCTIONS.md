# User Instructions

- 2026-08-29 | received: Codex/local | scope: ZEH8 engine worktree, `oleg_engine/` only | status: one-shot done 2026-08-29 | instruction: `You are the engine worker. Work in the worktree named for you below. Read everything, then act.` Build the engine under the binding shared CLI contract and ENGINE packet; use T1 planning with one Codex and one Opus pass in parallel; implement directly; use subagents heavily for independent tests; keep non-core features behind default-off flags until tested; write only inside `oleg_engine/`; never write `examples_test/`; create, commit, push, and open the specified PR within the hard time boxes; report commands, results, flags, blockers, questions, and PR URL. | context: Source conversation and message IDs are not exposed. The referenced `common.md` was absent, so the complete contract in the owner message was used. Delivered and pushed the engine package and PR 12. Independent lanes passed examples 01 to 02, example 03, T015, T016, exact evidence and coverage checks, idempotency, and both Codex and Claude backend paths. No corpus or out-of-scope file was changed. Owner session retains final signoff.
